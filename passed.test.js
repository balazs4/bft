test(`it should pass`, () => {
  const assert = require('assert').strict;
  assert.strictEqual(1, 1);
});

test(`it should also pass`, () => {
  const assert = require('assert').strict;
  assert.strictEqual(2, 2);
});
